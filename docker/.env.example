# رمز SA برای SQL Server در docker-compose.production.yml
SA_PASSWORD=Vapp@Secure2025!

# نگاشت پورت API (پشت Nginx: 127.0.0.1:8080:8080 | IP عمومی: 8080:8080)
API_PORT_MAPPING=8080:8080

# لوکال — callback پرداخت (اختیاری)
PUBLIC_API_BASE_URL=
PUBLIC_FRONTEND_URL=
